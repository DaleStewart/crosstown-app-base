# 2026-05-16T20:30:00Z — Banner (T007-followup service-advisor cassettes)

**Agent:** Bruce Banner (Tester)  
**Task:** T007-followup — hand-crafted service-advisor cassettes (Medium escalation, RED→GREEN)  
**Branch:** chore/deploy-hygiene  
**Status:** ✅ Complete  

## Deliverable

Authored 6 files (3 cassettes + 3 scenarios) to unblock eval gate for service-advisor prompts pending PR #27.

**Files:**
- `evals/orch_cassettes/OS-009.json` (1370 B) — `get_disruption_status` for prompt 4.
- `evals/orch_cassettes/OS-010.json` (1464 B) — `find_alternate_route` for prompt 5.
- `evals/orch_cassettes/OS-011.json` (1246 B) — `get_shuttle_bridging` for prompt 6.
- `evals/orch_scenarios/OS-009_disruption_status.yaml` (225 B).
- `evals/orch_scenarios/OS-010_find_alternate_route.yaml` (283 B).
- `evals/orch_scenarios/OS-011_get_shuttle_bridging.yaml` (271 B).

## Citation compliance

All cassettes honor the critical invariant: every response text includes ≥1 Citation token (`INC-XXXX`, `L-XXXXXX`, or `RB-XX-...`) matching orchestrator_runner's CITATION_REGEX.

**Citations used:**
- OS-009: INC-2001 (incident), L-201001 (log), RB-11-line-shutdown-contingency (runbook).
- OS-010: INC-2001, RB-11-line-shutdown-contingency.
- OS-011: INC-2001, RB-12-shuttle-bus-bridging.

## Verification

- JSON validity: ✅ all three cassettes.
- YAML validity: ✅ all three scenarios.
- Citation regex: ✅ ≥1 match per cassette.
- Orchestrator runner (hermetic): ✅ **11/11 PASS (0% failure)**.

## Tool contract caveats (flagged for Anvil)

PR #27's tool handlers not yet in local tree; cassette `tool_calls[].arguments` and `tool_calls[].result` shapes are inferred:

1. `get_disruption_status` arg: used `{"line": "L1"}` (spec consistent).
2. `find_alternate_route` arg: used `{"from": "S-Penn", "to": "S-East", "avoid_disruption": "..."}` vs. PR doc `(origin, destination, disruption_id?)` — **MISMATCH flagged**.
3. `get_shuttle_bridging` arg: used `{"disruption_id": "..."}` (spec consistent).
4. Result shapes: plausible envelopes; actual handlers may differ.

Recommendation: Anvil should re-record cassettes from live orchestrator + service_advisor post-PR#27 merge to confirm envelopes.

## Synthetic IDs

- Incident: `INC-2001` (not in data files; chosen to avoid collision with existing INC-1005/INC-1010).
- Log: `L-201001` (chosen outside existing L-000xxx/L-009xxx range).
- Runbooks: `RB-11-line-shutdown-contingency`, `RB-12-shuttle-bus-bridging` (match filenames in docs/service-disruption-advisor.md).

## Notes

- No live capture possible (PR #27 not deployed on this branch).
- Tool result shapes derived from `route_graph.json` schema (on PR #27).
- No git operations performed per task constraints.

---  
**Authored by:** Bruce Banner  
**Decision:** D-030
