# Orchestration Log · Okoye (Operations)

**Timestamp:** 2026-05-13T22:16:00Z
**Agent:** Okoye
**Role:** Operations & Delivery
**Mode:** Background
**Status:** ✓ SUCCESS

## Work Summary

Built the judging app deployment & operations scaffold:

- **Azure Declarative:** `apps/judging/azure.yaml` (azd manifest, static web app host, single service, environment schema)
- **Infrastructure:** Bicep parameters template `apps/judging/infra/main.parameters.json` (SKU, region, Cosmos throughput bounds)
- **Seed Data:** `scripts/seed-teams.js` (Node.js script to populate Cosmos with test teams) + `scripts/teams.csv` (CSV source data for hackathon tracks)
- **Documentation:** `apps/judging/README.md` (deployment walkthrough, local setup, azd vs. SWA CLI, admin-role grant path)
- **Source Control:** `.gitignore` additions for `.azure/` env directories, `local.settings.json` secrets, Function runtime artifacts

## Decision Reference

**D-004** (Separate `azure.yaml` inside `apps/judging/`, do not modify root): Adopted. Rationale: isolation of blast radius, different lifecycles (event-scoped vs. long-running), different service topology, team parallelism.

## Integration Points

- **azd lifecycle:** `cd apps/judging && azd up` deploys SWA + Cosmos + managed Functions as a single unit
- **Local dev:** `azd env list` shows available environments; `azd env select` picks one; `npm run build` in `api/` prepares Functions; `swa start --api-location ./api` runs locally
- **Secrets:** `local.settings.json.example` provided; seed script requires `COSMOS_ENDPOINT` and `COSMOS_KEY` from azd outputs

## Key Decisions

1. **Nested `azure.yaml`:** Not modifying root; users must `cd apps/judging` before azd commands
2. **SWA managed Functions:** No separate Function App resource; SWA auto-discovers `./api/` directory
3. **Seed data:** Test CSV includes two tracks (Azure AI, Copilot Studio) with 6 sample teams per track

## Follow-up Tasks

1. Replace `{{TODO_TENANT_GUID}}` and create AAD app registration
2. Decide standalone GH Actions workflow vs. azd integration (currently azd-native)
3. Document admin-role grant path (SWA role management vs. `ADMIN_EMAILS` env var)
4. Set up post-deployment script for running seed-teams.js

## Verification

- `azure.yaml` schema validation passes
- `infra/main.parameters.json` compiles (no syntax errors)
- `seed-teams.js` imports Cosmos SDK correctly
- `.gitignore` entries prevent accidental secret commits

---

**Logged by:** Scribe (Shuri)
**Session:** 2026-05-13-judging-app-build
