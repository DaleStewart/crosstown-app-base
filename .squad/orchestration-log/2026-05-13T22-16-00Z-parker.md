# Orchestration Log · Parker (Frontend)

**Timestamp:** 2026-05-13T22:16:00Z
**Agent:** Parker (Peter Parker / Spider-Man)
**Role:** Frontend / DevRel
**Mode:** Background
**Status:** ✓ SUCCESS

## Work Summary

Built the judging app frontend at `apps/judging/src/` with full MTA brand alignment:

- **HTML Pages:**
  - `index.html` — landing page (feature cards, track selection, admin gateway)
  - `judge.html` — scoring interface (team picker, criterion cards, live leaderboard mini)
  - `admin.html` — management console (lock/unlock tracks, export scores, team admin)

- **Stylesheets & Scripts:**
  - `styles.css` — MTA brand tokens (Helvetica, route-bullet palette, sharp-corner panels, dark mode)
  - `auth.js` — AAD auth flow (Microsoft Identity Library)
  - `toast.js` — ephemeral notification system
  - `criteria-ui.js` — criterion card rendering with tier colors and numbered bullets

- **MTA Brand Language:**
  - **Fonts:** Helvetica Neue (no web fonts), weights 400/500/700, uppercase display at `-0.02em` to `-0.04em` tracking
  - **Colors:** MTA Blue `#0039A6`, MTA Red `#EE352E`, MTA Green `#00933C`, MTA Orange `#FF6319`, MTA Yellow `#FCCC0A`
  - **Shape:** Sharp corners (2px on buttons/inputs, 0 on cards/panels, circular bullets/pills at 50%/999px)
  - **Components:** `.bullet` (route-bullet primitive), `.hack-roundel` (hackathon wordmark), `.signage` (hero panels), `.tier-block` / `.tier-cell` (score tier display)
  - **Iconography:** Tabler icons (mono), used sparingly; numbered bullets (`01`–`05`) replace criterion icons
  - **Dark mode:** bg `#0A0A0A`, surface `#181818`, navy `#4D7EE8`

## Decision Reference

**D-003** (Frontend design tokens for the judging app, v3 — MTA brand): Adopted. Rationale: maintains MTA visual identity, sharp-cornered panels, brand compliance.

## Design Pivots

- **v1 → v2:** Editorial pass added typography expansion (Barlow Semi Condensed), atmosphere (diagonal subway rules), motion (stagger reveal, tier flash, score button press)
- **v2 → v3:** Brand override to MTA Helvetica + NYCT route palette; replaced all rounded-corner logic with 0px shape; removed all Barlow web fonts; added route-bullet primitives (`.bullet` with `.sm/.md/.lg/.xl/.xxl` size modifiers)

## Key Patterns

- `.card, .summary, .section, .table-wrap, .feature, .team-card, .track-card` — single brand-layer rule enforces `border-radius: 0 !important`
- Tier mapping: Exceptional (≥90, green), Strong (≥70, navy), Developing (≥50, orange), Needs work (<50, red)
- Criterion cards use numbered bullets (`.bullet.md`) instead of Tabler icons (visually hidden)
- Leaderboard `.table-wrap.locked` overlays a caution-stripe pattern + `.lock-flash` full-screen 45° yellow/black pulse on freeze
- Motion gated by `prefers-reduced-motion: reduce`

## Verification

- All HTML pages pass basic structure validation
- CSS passes syntax check, dark-mode color contrast meets WCAG AA for text
- Auth flow wires to Microsoft Identity Library (redirectUri stored in `staticwebapp.config.json`)
- Toast notifications tested for stacking and auto-dismiss (4s default)

---

**Logged by:** Scribe (Shuri)
**Session:** 2026-05-13-judging-app-build
