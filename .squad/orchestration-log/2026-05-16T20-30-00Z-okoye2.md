# 2026-05-16T20:30:00Z — Okoye² (T107 deploy guard)

**Agent:** Okoye (Operations)  
**Task:** T107 — deploy.yml main-only guard (Large, FR-013)  
**Branch:** chore/deploy-hygiene  
**Status:** ✅ Complete  

## Deliverable

Added main-only deploy guard to `.github/workflows/deploy.yml` per FR-013 enforcement.

**Changes:**
1. New `workflow_dispatch.inputs.confirm_non_main` (boolean, default false).
2. First step (after checkout) validates: if not `refs/heads/main`, fail unless dispatch + override flag set.
3. Override warning uses `::warning::` with branch ref + actor for audit.

## Trace table (4 cases covered)

| Case | Event | Ref | Override | Result |
|---|---|---|---|---|
| 1 | push main | refs/heads/main | n/a | ✅ proceed |
| 2 | push feature | refs/heads/feature/foo | n/a | ❌ fail |
| 3 | dispatch non-main + override | refs/heads/chore/... | true | ⚠️ warning, proceed |
| 4 | dispatch non-main, no override | refs/heads/chore/... | false | ❌ fail |

Edge cases verified:
- Push events cannot smuggle the override (inputs not present on push).
- Boolean coercion correct (GitHub Actions serializes as "true"/"false" strings).

## Verification

- YAML parse: ✅ valid.
- yamllint: skipped (not present on Windows).
- Step order: ✅ step 2 (after checkout, before Install azd).
- No new external dependencies.

## Notes

- Plan §4 anticipated override audit richness as open question; current implementation is the documented Large baseline.
- env: passthrough used for github.* interpolations (GitHub recommended for script injection mitigation).
- No git operations performed per task constraints.

---  
**Authored by:** Okoye  
**Decision:** D-030
