# Orchestration Log · Stark (Architect)

**Timestamp:** 2026-05-13T22:16:00Z
**Agent:** Stark (Tony Stark / Iron Man)
**Role:** Architect
**Mode:** Background
**Status:** ✓ SUCCESS

## Work Summary

Built the judging app infrastructure scaffold at `apps/judging/`:

- **Infra:** `apps/judging/infra/main.bicep` (SWA Standard SKU, Cosmos DB serverless, partition key `/track`)
- **API:** 7 Functions under `apps/judging/api/` (all Node.js, passed `node --check`)
  - `teams-list/` — list all teams for a track
  - `teams-create/` — add a new team
  - `myscores/` — user's own scores across teams
  - `score-submit/` — record a judge's criterion score
  - `leaderboard/` — aggregate scores by team (partition-scoped query)
  - `lock/` — freeze judging for a track
  - `export/` — CSV export of final scores
- **Config:** `apps/judging/staticwebapp.config.json` (AAD route guards, managed Functions routing)
- **Shared:** `apps/judging/shared/criteria.js` (dual-export: CJS + window global for frontend inclusion)

## Decision Reference

**D-002** (Judging app layout — `apps/judging/` as a sibling workload): Adopted. Rationale: isolation of deploy lifecycle, single source of truth for criteria, SWA managed Functions minimizes infra surface.

## Key Patterns

- Partition key `/track` on every Cosmos container ensures partition-scoped queries are O(scores-in-track), predictable RU on serverless
- `api/_shared/criteria.js` is a one-line re-export (not a function, shared logic only)
- Frontend can `<script>`-include `../shared/criteria.js` without bundling
- SWA managed Functions auto-discover `./api/` directory

## Next Steps (Okoye)

1. Finalize azd integration vs. standalone GH Actions workflow targeting `apps/judging/infra/main.bicep`
2. Replace `{{TODO_TENANT_GUID}}` placeholder and create AAD app registration
3. Document admin-role grant path (SWA role management vs. `ADMIN_EMAILS` env var)

## Verification

- `node --check apps/judging/api/*/index.js` — all functions pass syntax check
- Bicep structure validated in `main.bicep` (variables, parameters, Cosmos container definitions)
- Route rules in `staticwebapp.config.json` match function paths and guard with AAD claims

---

**Logged by:** Scribe (Shuri)
**Session:** 2026-05-13-judging-app-build
