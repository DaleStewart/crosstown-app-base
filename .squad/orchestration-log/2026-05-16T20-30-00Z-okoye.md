# 2026-05-16T20:30:00Z — Okoye (T101 smoke-test.sh)

**Agent:** Okoye (Operations)  
**Task:** T101 — scripts/smoke-test.sh (Large, FR-009)  
**Branch:** chore/deploy-hygiene  
**Status:** ✅ Complete  

## Deliverable

Hand-authored `scripts/smoke-test.sh` — comprehensive post-deployment smoke test per FR-009 spec.

**Structure:**
- Positional arg: `<frontend-url>` (trailing slash stripped).
- Optional flag: `--full` (enables 6-prompt demo suite).
- JSON parsing: auto-detects jq or falls back to grep/sed.

**Baseline checks (5):**
1. GET `/` → HTTP 200 + "Crosstown" OR `<div id="root"` (SPA fallback).
2. GET `/api/health` → HTTP 200, `status ∈ {ok,degraded}`, `service == "orchestrator"`.
3. GET orchestrator `/health` (if `ORCHESTRATOR_URL` env set; else skip with warning).
4. POST `/api/turn` with `{"text":"status of L1?"}` → HTTP 200, non-empty `text` field.
5. (--full only) Iterate 6 rehearsed demo prompts from spec User Story 2 → each 200, non-empty `citations[]`.

**Exit codes:** 0 (all pass), 1 (check fail), 2 (usage error).

## Deviations from plan (all upgrades)

| Plan | Implemented | Why |
|---|---|---|
| `/healthz` | `/health` | Audit T001 confirmed `/health`. |
| Body marker SPA only | Crosstown OR SPA root | Task brief + app-specific marker. |
| `SERVICE_FRONTEND_URI` | script agnostic; hook uses `FRONTEND_URL` | Audit T004 confirmed actual var name. |
| Check 2: status only | + `service == "orchestrator"` | Task brief explicit; detects nginx rewrite to wrong upstream. |
| Check 3 = 3-service deps | direct GET `/health` optional | Task brief; service-advisor dependency surfaces in check 5 (--full). |
| Check 4 ping / assert citations | `status of L1?` / assert non-empty text | Task brief override. |
| Check 5 rerun 4 | 6 explicit demo prompts | FR-008 alignment. |

All deviations are upgrades, not relaxations.

## Verification

- bash -n syntax: ✅ clean.
- shellcheck: skipped (not installed on Windows).
- Negative test (bogus host): ✅ exit 1, correct error format.
- Live test (current deployment): ✅ correctly detects nginx rewrite not yet landed (check 2 fails as expected).
- --full branch: ✅ parses cleanly; not executed live (will fail on same nginx 404 until PR #25 merges).

## Notes

- mktemp usage: portable (GNU + macOS BSD).
- Bug found + fixed during verification: HTTP 000 double-write (both curl + fallback wrote to stdout). Replaced with `set +e / rc / set -e` pattern.
- No git operations performed per task constraints.

---  
**Authored by:** Okoye  
**Decision:** D-030
