# PR Triage — 2026-05-17

**Triage by:** 🏗️ T'Challa (Lead)
**Requested by:** Sean (segayle)
**Scope:** Track 2 voice/orchestrator stack (apps/orchestrator, apps/log_analyst, apps/frontend, evals, docs). The judging app (apps/judging) is out-of-scope — Okoye is deploying it.
**PRs reviewed:** 8 open (#19, #21, #25, #26, #27, #30, #31, #32)

## PR Triage — 2026-05-17

### #19 — fix(frontend): send stop frame to orchestrator on mic release
Status: 🔴 STALE / SUPERSEDED
Files: `apps/frontend/src/hooks/useVoiceSession.ts`, `e2e/mic-hold-release.spec.ts` (+ squad docs). 124+/7−.
CI: ❌ frontend job FAIL (pre-existing playwright duplicate-version error in `mic-button.spec.ts` — unrelated to PR content). Other 5 jobs green.
Recommendation: **close — superseded by #26**. PR #26 body explicitly calls out that #19 added the stop-frame to `disconnect()` (wrong place — never fires on release), and #26 puts it in `stopTalking()` (the correct call site).
Risk: low — closing loses no real fix; #26 already lands the correct behavior.

### #21 — feat(frontend): render user-turn transcripts in chat (P0 — conversation parity)
Status: 🟡 NEEDS REVIEW → ready after rebase
Files: `useVoiceSession.ts`, `protocol.ts`, +tests (protocol.test.ts, useVoiceSession.test.ts). 164+/2−.
CI: ❌ frontend job FAIL (same pre-existing playwright-dup error, NOT caused by this PR). Other 5 jobs green. D-032 in decisions.md already documents this PR's design + verifies 9/9 vitest locally.
Recommendation: **merge after #26 + rebase**. #26 fixes the playwright collection bug (`vite.config.ts` include glob); once #26 is in main, #21's CI will go green on rebase. Code already reviewed in D-032 — Parker has signed off and revision frontend--0000004 is live.
Risk: low — fix is targeted, well-tested, P0 for demo. Light conflict possible in `useVoiceSession.ts` against #26 — Parker to resolve.

### #25 — fix(frontend): restore spacebar PTT + audio capture + drop stale /api/health
Status: 🔴 STALE / SUPERSEDED
Files: PushToTalkButton.tsx, useVoiceSession.ts, audio.ts, nginx.conf, vite.config.ts (+ squad docs). 93+/9−. All 6 CI jobs ✅ green.
CI: ✅ green.
Recommendation: **close — superseded by #26**. #26 includes everything #25 does plus: orchestrator-side voice/foundry_realtime fixes (D-033), text-input feature (PR #23 carry-forward, D-034), additional adversarial-review catch (stream leak guard if `ctx.resume()` throws), and broader test coverage. Both branches address the same Sean UAT P0 bugs; #26 is the more complete take and is already deployed (revision frontend--0000007 live).
Risk: low — #26 is the canonical fix; keeping both invites merge conflicts.

### #26 — fix(frontend): restore spacebar PTT + audio capture + /api/health (P0 — Sean UAT demo 2026-05-19)
Status: 🟢 MERGE-READY
Files: 14 files across frontend (PushToTalkButton, TextInput, useVoiceSession, audio, App, nginx, vite.config) + orchestrator (settings, voice/factory, voice/foundry_realtime, tests). 630+/32−. anvil/adversarial-reviewed.
CI: ✅ all 6 jobs green (python log_analyst, python orchestrator, citation-gate, frontend, orchestrator-gate, bicep).
Recommendation: **MERGE FIRST**. Fixes three P0 UAT bugs (spacebar, audio frames, /api/health 404) for the Tuesday 2026-05-19 demo. Already deployed to frontend--0000007 and verified live. Carries D-033/D-034/D-035.
Risk: medium-low — large diff but every change is targeted at a specific reported bug; adversarial review caught the one stream-leak edge case and it's fixed. Verify post-merge: rerun frontend CI on PR #21 after rebase.

### #27 — feat: Service Disruption Advisor (rider-facing specialist, 4 tools)
Status: 🟡 NEEDS REVIEW (CI not run yet)
Files: 41 files. New `apps/service_advisor/` service (Dockerfile, 4 tools, data_loader, router, settings, 4 test suites), DisruptionBanner + AlternateRouteCard frontend components, orchestrator wiring (agent/tools.py, main.py, system_prompt.py, settings), eval scenarios SC-009/OS-009/010/011, red-team RT-009/010, infra/main.bicep update, azure.yaml. 1806+/32−.
CI: ⚠️ **no checks reported** on `anvil/feat-service-advisor` branch. Anvil is already on this — see `.squad/log/20260517144500-pr27-ci-fix.md`. Block on CI green before any merge.
Recommendation: **hold for CI fix + full code review**. Once CI is running and green, this needs a dedicated review pass by Stark (orchestrator wiring + tool registration table-driven contract) and Banner (eval/red-team scenarios + cassette parity). Don't merge same-day as #26 — sequence after the UAT-critical fixes land.
Risk: high — net-new specialist service, bicep + azure.yaml change → ACA deploy implications, touches orchestrator tool-routing path (load-bearing citation contract per architectural rule #2). Worth the careful review; do not rush.

### #30 — test: happy-path Playwright against live deploy
Status: 🔵 DRAFT/WIP
Files: `.github/workflows/playwright-live.yml`, `e2e/happy-path.spec.ts`, `playwright.config.ts` (+ squad orchestration logs). 2549+/6−. All 6 CI jobs ✅ green.
CI: ✅ green.
Recommendation: **skip — author (Stark) marked draft**. Don't review until un-drafted. Live-deploy smoke is nice-to-have for the demo, not a blocker.
Risk: low — gated by draft flag; no impact on main.

### #31 — docs: add status bars + version-control matrix to top-level docs
Status: 🟢 MERGE-READY
Files: README.md, docs/47doors-comparison.md, docs/evals.md, docs/redteam.md, docs/voice.md, playwright.config.ts (+ squad orchestration logs). 752+/34−.
CI: ✅ all 6 jobs green (duplicated entries in rollup but every conclusion is SUCCESS).
Recommendation: **merge after #26**. Pure docs polish from Ralph. No code surface touched in apps/. Light conflict risk with #32 only on shared `.squad/decisions.md` and `.squad/agents/*/history.md` files — sequence #31 first, #32 rebases.
Risk: low — docs only.

### #32 — docs(extensions): add status bars + version-control matrix to all 9 extension READMEs
Status: 🟢 MERGE-READY (after #31)
Files: 9 × `docs/extensions/0N_*/README.md` (+ shared `.squad/decisions.md`, agent histories, orchestration logs). 2461+/3−.
CI: ✅ 4 of 6 jobs reported, all SUCCESS (citation-gate/orchestrator-gate skipped via path filter — expected for docs-only changes).
Recommendation: **merge after #31 + rebase**. Pure docs from Ralph; complements #31. Will conflict with #31 on `.squad/decisions.md` and agent histories — merge #31 first, then rebase #32 (Ralph to resolve).
Risk: low — docs only, no apps/ surface.

---

## Merge order

| Order | PR | Action | Rationale |
|---|---|---|---|
| 1 | **#26** | MERGE FIRST | P0 UAT demo blocker (Sean), all CI green, deployed + verified. Unblocks #21 by fixing pre-existing playwright-dup CI bug. |
| 2 | **#25** | CLOSE | Superseded by #26. |
| 3 | **#19** | CLOSE | Superseded by #26 (stop-frame put in correct call site). |
| 4 | **#21** | REBASE → MERGE | P0 conversation parity. Rebase onto post-#26 main → frontend CI goes green → merge. Light conflict in `useVoiceSession.ts` — Parker resolves. |
| 5 | **#31** | MERGE | Docs polish, all green. |
| 6 | **#32** | REBASE → MERGE | Rebase onto post-#31 main to resolve `.squad/decisions.md` + agent-history conflicts. |
| 7 | **#27** | HOLD → REVIEW | Wait for Anvil's CI fix, then full review pass by Stark (orchestrator wiring) + Banner (eval/red-team). Largest risk surface — do not co-merge with UAT fixes. |
| 8 | **#30** | SKIP | Draft. Revisit when Stark un-drafts. |

**Top priority:** #26 — only thing standing between UAT and Tuesday's demo.

**Cleanup actions queued (await Sean authorization):**
- Close #19, #25 with link to #26.
- After #26 lands: ping Parker to rebase #21, ping Ralph to rebase #32.
- After #27 CI is green: assign Stark + Banner for review.
